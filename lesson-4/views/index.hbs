<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
          integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

    <title>{{title}}</title>
</head>
<body>

<div class="container my-5 entry">
    <div class="card">
        <div class="card-header">
            <h4><a href="/">#</a> {{cardHeader}}</h4>
        </div>
        <div class="card-body">
            <label for="rubric" class="h5 pb-2">{{menuHeader}}</label>
            {{#if lastC}}
                <p class="p-2 bg-info text-white"><small>Прошлый раз вы смотрели: {{lastC.rubric}}</small></p>
            {{/if}}
            <form action="/getnews" method="post">
                <div class="news-category form-group">
                    <select name="rubric" id="rubric" class="form-control">
                        <option value="sport">Спорт</option>
                        <option value="culture">Культура</option>
                        <option value="world">В мире</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-secondary">Получить</button>
            </form>
        </div>
    </div>
</div>

{{#if nameCategory}}
    <div class="container" id="content-news">
        <div class="card">
            <div class="card-header">
                <p class="h5">{{nameCategory}}</p>
            </div>
            <div class="card-body">
                <ul class="list-unstyled" id="listNews">
                    {{#each news}}
                        <li>{{this.title}}</li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
{{/if}}

</body>
</html>